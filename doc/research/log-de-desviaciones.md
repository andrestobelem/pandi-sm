# Log de desviaciones de pandi-sm

Date: 2026-06-28 (registro vivo — se actualiza por capa)

> **Status: REGISTRO VIVO.** Excepción a la convención de nombre con fecha de
> `doc/research/README.md`: este archivo es un registro acumulativo, no un
> documento de investigación cerrado. Lo establece L0 como **convención**; las
> entradas se confirman/añaden cuando cada capa implementa su decisión.

## Objetivo

Registrar toda divergencia **consciente** de pandi-sm respecto a su oráculo —ANSI
INCITS 319-1998 (draft v1.9), el dialecto Pharo/Squeak, o IEEE— para no confundir
un bug con una decisión, y para que el harness diferencial (§7 del plan) no marque
como fallo lo que es desviación deliberada.

## Convención de entrada

Una fila por desviación. Campos:

| Campo | Significado |
| --- | --- |
| `ID` | `DEV-NNN` estable, no se reusa. |
| `Desviación` | Qué hace pandi-sm distinto. |
| `Respecto de` | `ANSI` · `dialecto:pharo` · `dialecto:squeak` · `IEEE`. |
| `Origen` | `ingeniería` · `dialecto:<cuál>` · `spec-ANSI`. |
| `Dónde` | Sección del plan / capa que la decide. |
| `Estado` | `decidida` (en plan) · `implementada` (con test que la fija). |

**Regla:** toda fila `implementada` debe tener al menos un caso de test que la
fije; toda extensión no-ANSI en el AST/corpus lleva `origin='ext:pharo-squeak'`.

## Registro

| ID | Desviación | Respecto de | Origen | Dónde | Estado |
| --- | --- | --- | --- | --- | --- |
| DEV-001 | `copy` de `<Object>` es **shallow** (no contrato ANSI). | ANSI | ingeniería | §5.2 (L2) | decidida |
| DEV-002 | `Float / 0` señala `ZeroDivide` (no devuelve `inf`/`nan`). | IEEE | ingeniería | §8.2 (L4) | decidida |
| DEV-003 | Condicionales/lógicos (`ifTrue:`/`and:`/`or:`/`not`) NO se inlinean: son envíos reales (preserva `doesNotUnderstand:` de un no-Boolean). Squeak SÍ los inlinea. | dialecto:squeak | ingeniería | §5.3.1 (L3) | decidida |
| DEV-004 | `timesRepeat:` se implementa vía `to:do:` (Squeak no lo inlinea; el Blue Book usa `whileTrue:` interno). | dialecto:squeak | ingeniería | §5.3.1 (L3) | decidida |
| DEV-005 | Recursión profunda no-bucle limitada por el stack de V8 (sin TCO); `RangeError` se mapea a `Error` Smalltalk señalable. Sin gate sobre profundidad concreta. | — (límite de plataforma) | ingeniería | §5.3.1 (L3) | decidida |
| DEV-006 | `ByteString` vs `WideString` no se distingue en el MVP (`String` sobre UTF-16 de V8, iterado por code point). Pharo/Squeak sí distinguen. | dialecto:pharo | ingeniería | §5.0/§5.1, doc decisiones modelo | decidida |
| DEV-007 | Sin imagen: el kernel se carga desde assets `.st` (estilo Amber), no desde un snapshot. | dialecto:squeak | ingeniería | §2/§5.4.0 | decidida |
| DEV-008 | Raíz de la jerarquía = `Object` (superclase `nil`); `ProtoObject` diferido. | dialecto:pharo | spec-ANSI | §5.2 (L2) | decidida |
| DEV-009 | `3 --4` parsea VÁLIDO como `MessageSend(3,'--',[4])` (no error). Corrige mis-lectura del gate §5.1 que lo listaba como negativo; `--` es `binarySelector` por maximal-munch (el `doesNotUnderstand:#--` es de L2). Sin `E_NEG_NO_SPACE`. | ANSI (corrección de plan) | spec-ANSI | §5.1 / L1-decisiones CORR-1 | decidida |
| DEV-010 | `a:=b` parsea VÁLIDO como `Assignment` (no error). Corrige mis-lectura del gate §5.1; el lexer prefiere `:=` pegado. Sin `E_ASSIGN_VS_KEYWORD`; el negativo real es `3 := 4` (target literal) → `E_UNEXPECTED_TOKEN`. | ANSI (corrección de plan) | spec-ANSI | §5.1 / L1-decisiones CORR-2 | decidida |
| DEV-011 | scaledDecimal no tiene forma léxica "malformada" bajo maximal-munch (`s` siempre cierra con scale-default); el item "scaledDecimal malformado" del gate §5.1 es vacuo y se elimina. | ANSI (corrección de plan) | ingeniería | §5.1 / L1-decisiones R7 | decidida |
| DEV-012 | Las `temporaries` viven en `SequenceNode` (único hogar; bloque y programa), no en `BlockNode` como listaba §5.1. `BlockNode = {params, body, span}`. | dialecto (forma AST) | ingeniería | §5.1 / L1-decisiones R13 | decidida |
| DEV-013 | `span.offset` = índice de unidad UTF-16 (slice-reversible); `span.column` = code points (1 por surrogate pair). Pueden divergir en el plano astral. | — (modelo interno) | ingeniería | L1-decisiones R1 | decidida |
| DEV-014 | `_` admitido como `letter` en `identifier` (de-facto Pharo/Squeak; ANSI sólo lista A-Za-z). Identifiers con `_` se marcan origin ext donde aplique. | dialecto:pharo | ingeniería | L1-decisiones R3 / léxico | decidida |
| DEV-015 | Un `:` aislado (no seguido de `=`) lexea como token `colon`, **no** `E_UNEXPECTED_CHAR`. Es el marcador de argumento de bloque `[:x \| …]` (R13); un `:` fuera de contexto lo rechaza el PARSER (`E_UNEXPECTED_TOKEN`), no el lexer. Corrige la contradicción interna de R3/R10 (que lo listaban como `E_UNEXPECTED_CHAR`) con R13. | ANSI (corrección de contrato) | ingeniería | L1-decisiones R3/R10/R13 | implementada (`test/L1/lexer-slice1.test.ts`: `:` / `[:x`) |
| DEV-016 | `byteArrayOpen` (`#[`) NO abre posición de operando (R2): `#[-4]` ⇒ `#[` `-`(binarySelector) `4`, mientras `#(-4)` ⇒ `#(` `-4`. Los elementos de un byteArray son bytes sin signo `[0,255]` (un negativo lo rechaza el parser con `E_BYTE_RANGE`); asimetría deliberada con `arrayOpen`. | ANSI/ingeniería | ingeniería | L1-decisiones R2 | implementada (`test/L1/lexer-slice4.test.ts`: `#[-4]` vs `#(-4)`) |
| DEV-017 | Float no finito por overflow IEEE-754 (`1e400` ⇒ `Infinity`) se serializa en `astToJSON` como envoltura `{"$float":"Infinity"\|"-Infinity"\|"NaN"}`, espejo de `{"$bigint":…}`. `JSON.stringify(Infinity)` da `"null"`, lo que corrompería en silencio los golden del corpus (R12). El lexer conserva `value=Infinity` (R4: `value=parseFloat`). | IEEE/JSON | ingeniería | L1-decisiones R4/R12 | implementada (`test/L1/ast-to-json.test.ts`, `lexer-slice2.test.ts`: `1e400`) |
| DEV-018 | Los **radix-float** (`16r1.8`, `2r1.1`) quedan fuera de alcance en L1: R4 sólo define radix enteros. El lexer parte `16r1.8` ⇒ `16r1` `.`(period) `8`. ANSI permite radix floats; pandi-sm los difiere. | ANSI | ingeniería | L1-decisiones R4 | implementada (`test/L1/lexer-slice2.test.ts`: `16r1.8`) |
| DEV-019 | El parser (descenso recursivo sin TCO) no tiene límite de profundidad fijo: una anidación patológica desborda el stack de V8. Para honrar R10 ("`parse()` nunca lanza, devuelve `{ast,errors}`"), `parse()` envuelve el descenso en try/catch y mapea el `RangeError` a un `E_NESTING_LIMIT` estructurado (`ast:null`); cualquier otra excepción se re-lanza. El umbral exacto depende de la plataforma (no determinista entre plataformas), pero SIEMPRE produce error estructurado, nunca excepción. Análogo a DEV-005 para el evaluador (L3). | — (límite de plataforma) | ingeniería | L1-decisiones R10 | implementada (`test/L1/parser-recovery.test.ts`: anidación 50k ⇒ `E_NESTING_LIMIT`) |
| DEV-020 | `super` evaluado a nivel de programa (fuera de un método, sin `definingClass`) lanza un `Error` de host DETERMINISTA (`variable no resoluble: super`), no enruta por `doesNotUnderstand:`. `super` sólo tiene semántica dentro de un método (donde hay un `definingClass` desde el que arrancar el lookup por la superclase); a tope de programa no existe ese contexto. Comportamiento ANSI-*undefined*; el error es observable y no corrompe. | ANSI (*undefined*) | ingeniería | §5.4.0 (KERNELLOAD/L3.5) | decidida |
| DEV-021 | El cargador `.st` separa los `method-defs` escaneando cabeceras `Name >> selector` sin stripear primero las cadenas entre comillas simples; una cadena de atributo del `class-def` que contuviera la secuencia `>>` (p.ej. `package: 'A>>B'`) se mis-parsearía. Limitación conocida DIFERIDA: los nombres de paquete/ivar reales del kernel no contienen `>>`, y una entrada así falla RUIDOSAMENTE (`KernelLoadError`/error de carga), nunca en silencio. Se cierra cuando el escaneo de cabeceras sea consciente de strings. | — (límite del cargador) | ingeniería | §5.4.0 (KERNELLOAD/L3.5) | decidida |
| DEV-022 | Las acciones del handler (`return`/`return:`/`retry`/`retryUsing:`/`resume`/`resume:`/`pass`) se implementan como un TERCER objeto de control-flow lanzado (`HandlerActionSignal`, capturado en `signalException`), NO como el **valor de retorno** del handlerBlock que prescribía §5.5 línea 513 ("reducir a 2 mecanismos"). El verify⇄repair descubrió que una acción del handler debe ser una SALIDA NO-LOCAL inmediata del handlerBlock (p.ej. `e return: 5` aborta el resto del handler), cosa que un valor de retorno NO logra (sólo sería el valor de la última expresión). El throw dedicado es por tanto MÁS correcto que el modelo de 2 throws del plan; sigue siendo plano (no `extends Error`, V8-2). | plan §5.5 (corrección de implementación) | ingeniería | §5.5/§5.5.1 (L5) | implementada (`test/L5/on-do-signal.test.ts`, `resume-retry.test.ts`) |
| DEV-023 | `resume`/`resume:` sobre una excepción NO-resumable (`Error`, `Exception` base) señala un Error de host concreto (`resume de excepción no resumable: <Clase>`). ANSI declara este caso *erroneous* (indefinido); fijarlo como aserción binaria es decisión de implementación sobre comportamiento *erroneous* (misma disciplina §8.3 que DEV-005/home-muerta). El test es binario pero su origen NO es spec-ANSI. | ANSI (*erroneous*) | ingeniería | §5.5 (L5) | implementada (`test/L5/resume-retry.test.ts`) |
| DEV-024 | `signalerContext` es opaco/implementation-defined en el MVP (Context reificado / `thisContext` diferidos); NO se asevera igualdad sobre él. El setter `messageText:` como selector también queda DIFERIDO a pulido L6 (fuera del gate de 12 selectores; el getter `messageText` y el seteo vía `signal:` sí existen). | dialecto/MVP | ingeniería | §5.5 (L5) | decidida |
| DEV-025 | El estado de instancia de excepción (`messageText`, `pendingAction`, `activeHandler`) se almacena en un side-Map JS por-instancia, NO en ivars indexadas, porque `instSize` NO es acumulativo por la cadena de superclases: una subclase de `Exception` queda con `instSize` = sólo sus ivars propias (0 si no declara), y `basicNew` no reservaría el slot heredado de `messageText`. Workaround que sortea esa limitación sin reescribir el modelo. **LIMITACIÓN RAÍZ CERRADA en L4:** `subclass:instanceVariableNames:` (y la pasada-1 del cargador `.st`) ahora SUMAN el `instSize` de la superclase (`instSize` = ivars propias + `superclass.instSize`) en los cuatro sitios; observable vía `instVarAt:` sobre una subclase 2-niveles. El side-Map de excepciones (`exceptions.ts`) PERMANECE intacto (ortogonal: cierra la raíz, no migra el workaround existente). | — (límite del object model) | ingeniería | §5.5 (L5); raíz cerrada en §5.4/L4 | implementada (raíz cerrada) `test/L4/f4-array.test.ts` |
| DEV-026 | Extensión Booleana F1: `&` `\|` `xor:` (en `True`/`False`, args Booleanos EAGER, NO bloques) e `ifNil:`/`ifNotNil:`/`ifNil:ifNotNil:` (en `UndefinedObject`). NO están en ANSI core; son la familia no-cortocircuito (`&`/`\|`) más el protocolo nil de Pharo/Squeak. `and:`/`or:`/`ifTrue:`/`ifFalse:`/`ifFalse:ifTrue:` PRE-EXISTEN de L3 (no se re-añaden). | dialecto:pharo-squeak | ingeniería | §5.4 (L4 F1-ext) | implementada (`test/L4/f1-boolean-nil.test.ts`) |
| DEV-027 | `Integer>>/` NO-exacta devuelve un `Float` en vez de un `Fraction` (`7 / 2` ⇒ `3.5`, no `7/2`). `Fraction` está DIFERIDA (no hay clase de racionales en el MVP); la división exacta (`8 / 2` ⇒ `4`) sí devuelve un Integer. ANSI prescribe `Fraction`. | ANSI | ingeniería | §5.4 (L4 F2) | implementada (`test/L4/f2-number.test.ts`) |
| DEV-028 | `Float` y `Character` son STObjects BOXED (`{class}` + campo dedicado `floatValue`/`codePoint`) mientras `SmallInteger` sigue NATIVO (`number`/`bigint`). El double del Float vive en `floatValue` (NO en un slot `pointers`, esquivando DEV-025); el code point del Character en `codePoint`, derivado del valor STRING del lexer vía `value.codePointAt(0)`. `classOf` gana las ramas vía `v.class`. Boxing = elección simple/depurable (karpathy) que deja intacto el hot-path de SmallInteger. | dialecto/MVP (representación interna) | ingeniería | §5.4 (L4 F2); §2 (no-boxing hot path) | implementada (`test/L4/f2-number.test.ts`) |
| DEV-029 | Una sola clase `Float`: las variantes `FloatE`/`FloatD`/`FloatQ` (sufijos `e`/`d`/`q` del literal) colapsan a `Float` en el MVP (el `floatKind` del lexer se descarta al boxear). ANSI/Pharo distinguen precisiones; pandi-sm las difiere. | ANSI/dialecto | ingeniería | §5.4 (L4 F2) | implementada (`test/L4/f2-number.test.ts`) |
| DEV-030 | Coerción mixta `bigint`+`Float` es LOSSY: el `bigint` se colapsa con `Number(bigint)` antes de la aritmética Float, perdiendo precisión más allá de 2^53-1. Aceptable en el MVP; la torre de coerción/generalidad limpia `bigint`↔`Float` está DIFERIDA. | ANSI (generalidad numérica) | ingeniería | §5.4 (L4 F2) | decidida |
| DEV-031 | Un `\|` AISLADO lexea como `verticalBar` (delimitador de temporaries, R10), NUNCA como `binarySelector`; runs más largos (`\|\|`, `<=`) sí son `binarySelector`. CONSECUENCIA: `a \| b` NO es expresable como envío binario de superficie (el parser lo lee como inicio de declaración de temporaries) — el selector `\|` (Boolean F1-ext, DEV-026) existe e es alcanzable SÓLO vía `send`/`perform:`, no por sintaxis. ANSI admite `\|` como selector binario en expresión; el oráculo diferencial (`gst`/Pharo) parsea `a \| b`, así que se registra para que no lo marque como fallo. | ANSI | ingeniería | L1-decisiones R10; aflora en §5.4 (L4 F1-ext) | implementada (`test/L4/f1-boolean-nil.test.ts` cabecera + `send`; lexer R10) |
| DEV-032 | `species` = `Array` para TODO receptor secuenciable: `collect:`/`select:`/`reject:`/`,`/`copyFrom:to:` devuelven SIEMPRE un `Array` fresco, incluso desde un `Interval` (`(1 to: 3) collect: […]` ⇒ `Array`) o una `OrderedCollection`. ANSI prescribe `species` como selector de metaclase configurable por clase; el MVP lo HARDCODEA a `Array` (no hay selector `species` real). Convención de ingeniería, NO ANSI core. | ANSI (§8.10) | ingeniería | §5.4 (L4 F3/F4) | implementada (`test/L4/f3-collection.test.ts`, `f4-sequenceable-interval.test.ts`) |
| DEV-033 | Las colecciones indexadas son `1`-based: `at:`/`at:put:`/`first`/`copyFrom:to:` indexan desde `1` (no `0`), y un índice fuera de `1..size` SEÑALA el `Error` genérico (NO `SystemExceptions.IndexOutOfRange`, ausente en el MVP — DRIFT-3), capturable por `on: Error do:`. Coincide con Smalltalk estándar (origin=dialecto vs el `0`-based de JS). | dialecto | ingeniería | §5.4 (L4 F4) | implementada (`test/L4/f4-array.test.ts`, `f4-sequenceable-interval.test.ts`) |
| DEV-034 | Los tres constructores de array de superficie evalúan a `Array`: `{ … }` (DynamicArray, elementos evaluados), `#( … )` (literal-array, elementos LiteralNode reificados) y `#[ … ]` (byteArray) ⇒ `Array` de `SmallInteger`. `ByteArray` como clase distinta está DIFERIDA (`#[1 2 3]` NO es un `ByteArray` reificado en el MVP, sólo un `Array`). DENTRO de `#( )` las pseudo-vars `true`/`false`/`nil` son LiteralNodes (`lit:'true'/'false'/'nil'`) y reifican a los MISMOS singletons que el top-level (`lookupGlobal`): `true`/`false` nativos, `u.nil`; los identifiers bare-word reifican a `Symbol` (`#(a b c)` ⇒ `#(#a #b #c)`). Sin esas ramas, `#(true false nil)` —sintaxis Smalltalk de base— caía a un `throw` de host NO capturable (mismo class-de-defecto que el resto del nodo: input válido que crashea el host). | ANSI/dialecto | ingeniería | §5.4 (L4 F3/F4) | implementada (`test/L4/f4-array.test.ts`) |
| DEV-035 | `Interval` sólo soporta extremos/paso ENTEROS SEGUROS (cabe en `2^53-1`). Un paso o extremo `Float` (`1 to: 2 by: 0.5`) o un `bigint` fuera del rango seguro (`10^21`) NO se soporta y SEÑALA un `Error` capturable, en vez de miscomputar en silencio (paso `Float` ⇒ `Number(STFloat)`=`NaN` ⇒ Interval vacío; `bigint` enorme ⇒ colapso de precisión vía `Number(bigint)` + bucle de impresión sin fin). El `Interval` de no-enteros/fracciones está DIFERIDO; el rechazo es RUIDOSO. | ANSI/MVP (representación interna) | ingeniería | §5.4 (L4 F4) | implementada (`test/L4/f4-sequenceable-interval.test.ts`) |
| DEV-036 | `to:do:`/`to:by:do:` se reconocen como FORMA ESPECIAL de bucle (eval.ts, antes del dispatch) SÓLO cuando el bloque es un `BlockNode` literal (plan §5.3.1, por la semántica de retorno-no-local y rendimiento). Con un bloque ALMACENADO/no-literal (`b := [:i | …]. 1 to: 5 do: b`) la forma especial NO dispara y el envío enruta a `doesNotUnderstand:` (`SmallInteger no entiende #to:do:`), CAPTURABLE como `MessageNotUnderstood` (ruta L5 limpia, no crash de host). ANSI define `Number>>to:do:` como MÉTODO real (funcionaría con un bloque-variable); pandi-sm DIFIERE ese método-fallback. El oráculo diferencial (`gst`/Pharo) ejecuta `1 to: 5 do: aBlockVar`, así que se registra para que L6 no lo marque como fallo inexplicado (mismo criterio que DEV-031). | ANSI | ingeniería | §5.3.1 (L3); aflora en §5.4 (L4 F4) | decidida (fallback `Number>>to:do:` diferido) |
| DEV-037 | `String` pasa de VALOR NATIVO JS a un `STObject` BOXED (`{class:u.String, hash, format, pointers:[], chars}`) vía `makeString(chars,u)`+`isString(v)` (espejo EXACTO de `STArray.elements`/DEV-028, una sola representación por clase). ÚNICA forma de satisfacer GATE-F5 (`'foo' == 'foo' copy` debe ser FALSE): los strings JS nativos son value-idénticos, la identidad es imposible sin boxear. El `chars` (string JS) es el campo dedicado; `pointers` queda vacío. La frontera NATIVA interna PERMANECE: claves de la tabla de símbolos, `STClass.name` (storage), `STSymbol.text`, construcción interna de texto de error, y el `printString()` bridge (devuelve un string host para `===`). Los literales String (`eval.ts`), `Class>>name`, `Symbol>>asString` y `Exception>>messageText` (vía `signal:`) son la capa de VALOR boxeada. SmallInteger sigue NATIVO (hot path intacto). | dialecto/MVP (representación interna) | ingeniería | §5.4 (L4 F5); §2 (no-boxing hot path) | implementada (`test/L4/f5-string-boxing.test.ts`, `f5-string-symbol.test.ts`) |
| DEV-038 | `test/L4/f0-identity.test.ts` (casos 3, "= vs ==", "'a' identityHash") se CORRIGIÓ de la semántica vieja *string-inmediato (por valor)* a la nueva *string-boxed (identidad por referencia)* mandada por GATE-F5: `'a' == 'a'` => FALSE (dos cajas distintas), `'a' = 'a'` => TRUE (contenido), `('a' identityHash) = ('a' identityHash)` => FALSE (hash por-objeto), mientras un MISMO binding conserva su `==`/`identityHash`. `test/L3/eval.test.ts` ("literal string 'hi'") se corrigió análogamente: un literal ya es un STObject boxed, no un nativo (`expect(typeof v).toBe('object')` + `printString === 'hi'`). NO es una regresión "rota" sino la corrección semántica que el plan exige. | plan §5.4 GATE-F5 (corrección de contrato) | ingeniería | §5.4 (L4 F5/F0) | implementada (`test/L4/f0-identity.test.ts`, `test/L3/eval.test.ts`) |
| DEV-039 | `Object>>printString` (la PRIMITIVA `objectPrintString`) sigue devolviendo un string JS NATIVO en el MVP (NO un String boxed), distinto del `print.ts` HOST bridge (que sí gana una rama `isString` para desenvolver una caja). Boxear el retorno de la primitiva rompería 6 asertos de `object-protocol.test.ts:312-321` (`send(3,'printString') === '3'`) sin beneficio para GATE-F5/F6 (ningún gate envía mensajes al RESULTADO de `printString`). CONSECUENCIA: un resultado de `printString` no puede recibir mensajes hasta que se boxee. DIFERIDO. | MVP (corrección de implementación) | ingeniería | §5.4 (L4 F5) | SUPERSEDIDA por DEV-048 (el diferir reabría un hueco de GATE-L4-IDENTITY; ahora se boxea) |
| DEV-040 | Una sola clase `String` en el MVP: la distinción `WideString`/`ByteString` NO se modela (los chars viven en un string JS, sin separar ancho de byte). Symbol sí es subclase de String (hereda `,`/`size`/`=`; sobreescribe `asString`). ANSI/Pharo distinguen variantes de ancho de carácter; pandi-sm las difiere (igual criterio que DEV-029 para Float). | ANSI/dialecto | ingeniería | §5.4 (L4 F5) | implementada (`test/L4/f5-string-symbol.test.ts`) |
| DEV-041 | `String>>,` y `String>>size` son protocolo PROPIO de String (primitivas a nivel de `chars`), NO heredado de `SequenceableCollection`: `'foo' , 'bar'` devuelve un STRING (`makeString`), no un Array (a diferencia de DEV-032, donde el `,` secuenciable devuelve siempre Array). String NO se re-parenta bajo `ArrayedCollection`/`SequenceableCollection` en el MVP precisamente para no heredar el `,` Array-returning (DRIFT-6); `String>>asString` es `^self` (.st puro, tag de procedencia `l4-strings`, GATE-L4-PROVENANCE). Compartir el protocolo de colección de Array para String queda DIFERIDO. | ANSI (§8.10 species) / dialecto | ingeniería | §5.4 (L4 F5) | implementada (`test/L4/f5-string-symbol.test.ts`) |

| DEV-042 | Los retornos UNSPECIFIED del protocolo `Stream` se fijan en pandi-sm (ANSI los deja implementation-defined): `next` pasado el final (`atEnd`) devuelve `nil` (NO señala error — leer un stream agotado es benigno, a diferencia de `at:` fuera de rango); `nextPut: anObject` devuelve `anObject` (el objeto escrito); `reset` devuelve `self`. La `position` es 0-based (índice del PRÓXIMO elemento), espejo del campo dedicado de `Interval`/`Array` (NO ivar con nombre, no cableado). NO se asevera igualdad sobre el retorno de `next` pasado el final en el gate; se documenta como prosa. | ANSI (retornos unspecified) | ingeniería | §5.4 (L4 F6, plan línea 745) | implementada (`test/L4/f6-stream.test.ts`) |
| DEV-043 | La jerarquía `Stream` (`Stream` <- `PositionableStream` <- `ReadStream`/`WriteStream`/`ReadWriteStream`) es EN MEMORIA únicamente: cada stream es DUEÑO de su propio buffer JS (`STStream {buffer, position, species}` vía `makeStream`), NO depende de `String new` ni de un String growable (DRIFT-7). `on: aCollection` toma una COPIA del Array (`elements`) o del String (chars -> Characters boxed), recordando la especie para que `contents`/`upToEnd` rindan la colección de respaldo (`on: ''` => String, `on: #()` => Array). `WriteStream on:` arranca VACÍO conservando sólo la especie (sobreescribe desde el inicio, ANSI). File/IO streams reales (`FileStream`, `stdin`/`stdout`) quedan DIFERIDOS. | ANSI/MVP (sólo in-memory) | ingeniería | §5.4 (L4 F6) | implementada (`test/L4/f6-stream.test.ts`) |
| DEV-044 | REPARACIÓN: los cuerpos de String que un `Symbol` (< String) HEREDA (`=`, `,`, `size`, `asSymbol`) leían SÓLO el campo `.chars` del String boxed, así que con un RECEPTOR `Symbol` (plain `{text}`, sin `.chars`) caían a `"" `/`no igual`. Se introduce `textOf(v)` (en `object.ts`, exportado del barrel) + la guarda `isSymbol(v)` que desenvuelven String boxed (`.chars`) **o** Symbol (`.text`) a un único string JS; las primitivas pasan a usarlo. Esto CORRIGE: `#foo = #foo` (era `false`, no reflexiva — `Symbol no es String boxed`) y `#foo ~= #foo` (era `true`); el gate verde no lo veía porque las pruebas de Symbol sólo ejercitaban `==`/identidad, nunca `=` por contenido con receptor Symbol. | ANSI (Symbol < String hereda `=`) | ingeniería | §5.4 (L4 F5, GATE-F5) | implementada (`test/L4/f5-string-symbol-repair.test.ts`) |
| DEV-045 | REPARACIÓN: `Symbol>>asSymbol` (heredado de String) devolvía el símbolo VACÍO `#` en vez de `^self`: `stringAsSymbol` interneaba `receiver.chars` y un Symbol no tiene `.chars`, así que interneaba `""`. Con `textOf` el receptor Symbol re-interna su `.text` y devuelve el MISMO objeto interned (`#foo asSymbol == #foo` => `true`; ANSI `Symbol>>asSymbol` es `^self`). | ANSI | ingeniería | §5.4 (L4 F5, GATE-F5) | implementada (`test/L4/f5-string-symbol-repair.test.ts`) |
| DEV-046 | REPARACIÓN: se OVERRIDE `String>>hash` (heredado por Symbol) para hashear por CONTENIDO, restaurando el invariante `a = b ⟹ a hash = b hash` (contrato Dictionary/Set). El default `Object>>hash` es el `identityHash` POR OBJETO (contador monótono), así que `'foo' hash ~= 'foo' copy hash` y dos cajas iguales mis-bucketean. El nuevo `String>>hash` reusa el hash estilo Java de `identityHash` aplicado al texto desenvuelto (`textOf`). `identityHash` (POR OBJETO) se conserva intacto — sólo `hash` pasa a contenido (consistente con DEV-038, que justifica el `identityHash` por-objeto, no el `hash`). | Smalltalk (contrato hash/=) | ingeniería | §5.4 (L4 F5, GATE-L4-IDENTITY) | implementada (`test/L4/f5-string-symbol-repair.test.ts`) |
| DEV-047 | REPARACIÓN: `String class>>new` se OVERRIDE para devolver `makeString('', u)` (el String boxed VACÍO), no el `basicNew` de Object class (que daba un STObject SIN campo `chars` sobre el que `isString()` falla y `size`/`,`/`=` misfire — instancia silenciosamente rota). ANSI `String new` (sin tamaño) es la cadena vacía. El String mutable growable completo (`at:put:` que crece la cadena) queda DIFERIDO (no lo pide F5; mismo criterio de alcance que el resto del protocolo String diferido). | ANSI/MVP | ingeniería | §5.4 (L4 F5) | implementada (`test/L4/f5-string-symbol-repair.test.ts`) |
| DEV-048 | REPARACIÓN (revierte DEV-039): `objectPrintString` ahora BOXEA su retorno (`makeString(hostPrintString(receiver), u)`), de modo que el SEND `x printString` devuelve un String boxed (capa de valor de usuario), no un string JS nativo. DEV-039 había diferido esto creyéndolo sin beneficio de gate, pero reabría el HUECO de value-identity que el boxing de String cerró para los literales: `3 printString == 3 printString` daba `true` (dos nativos JS `===`-iguales — viola GATE-L4-IDENTITY "objetos distintos con mismo contenido → == false"), y `(3 printString) asSymbol` daba `#` vacío (`textOf` de un nativo es `null`, internaba `''` — respuesta silenciosamente errónea), con `=`/`size`/`,` incoherentes sobre el resultado. `print.ts` HOST bridge sigue siendo la fuente ÚNICA de la representación; sólo se BOXEA su salida al cruzar a usuario. Coste citado por DEV-039 (6 asertos en `object-protocol.test.ts:308-322` que comparaban `send(...,'printString')` con un string JS) corregido envolviendo con `printString(...)` (la expectativa correcta para un retorno boxed). | plan §5.4 GATE-L4-IDENTITY (corrección de contrato) | ingeniería | §5.4 (L4 F5) | implementada (`test/L4/f5-printstring-boxing-repair.test.ts`) |
| DEV-049 | DIFERIDA (minor de robustez): `ReadStream` HEREDA `nextPut:` de `Stream` y deja MUTAR su buffer (`(ReadStream on: #(1 2 3)) nextPut: 9` escribe). ANSI `ReadStream` es de SÓLO LECTURA (`nextPut:` debería `shouldNotImplement`/dNU). Fuera de GATE-F6 (sólo exige presencia de los 5 selectores + round-trip de WriteStream); arreglarlo bien implica instalar `nextPut:` sólo en `WriteStream`/`ReadWriteStream` o añadir una bandera de escritura, con riesgo de regresión sobre el round-trip ya verde. Se DIFIERE para no exceder el alcance de la reparación (sólo blocker/major); registrado para que L6 no lo marque como hueco inexplicado. | ANSI (ReadStream read-only) | ingeniería | §5.4 (L4 F6) | decidida (guard read-only de ReadStream diferido) |
| DEV-050 | DIFERIDA (minor de adyacencia): `Character>>asString` no existe, así que consumir chars de un stream sobre String (`(ReadStream on: 'ab') next asString`) hace dNU. F5 es String+Symbol y Character es F2; añadir `Character>>asString` queda fuera del alcance de esta reparación (sólo blocker/major). Se DIFIERE y registra como hueco de protocolo de Character conocido. | ANSI/MVP | ingeniería | §5.4 (L4 F6, adyacente F2) | decidida (Character>>asString diferido) |
| DEV-051 | `Symbol>>=` y `Symbol>>~=` se OVERRIDE a IDENTIDAD (`==`), en vez del `=` por contenido que Symbol heredaría de String (DEV-044). ANSI/Pharo: un Symbol es único por interning, su `=` ES `==`. CONSECUENCIA con la asimetría deliberada de Smalltalk: `#foo = #foo` true (mismo objeto interned), `#foo = 'foo'` FALSE (un String no es el Symbol), PERO `'foo' = #foo` true (`String>>=` compara contenido y un Symbol < String aporta su `.text`). Antes (sólo DEV-044) `#foo = 'foo'` daba true — divergía del oráculo; este override lo ALINEA con gst/Pharo. El `~=` propio es necesario porque `String>>~=` llama a `stringEquals` directamente (no por dispatch). | ANSI/dialecto:pharo (alineación) | ingeniería | §5.4 (L4 F5) | implementada (`test/L4/f5-string-symbol.test.ts` identidad 4/5) |
| DEV-052 | DIFERIDA: las colecciones (`Array`/`OrderedCollection`/`Interval`) NO tienen `=` por CONTENIDO en el MVP: heredan `Object>>=` por identidad, así que `#(1 2) = #(1 2)` da FALSE. CONSECUENCIA observable en streams: `(WriteStream on: #()) ... contents = (... contents)` es false porque cada `contents` materializa un Array fresco. ANSI/Pharo: `SequenceableCollection>>=` compara tamaño + elementos por `=`. Se DIFIERE a una slice de igualdad de colecciones (o a L6); registrado para que el oráculo diferencial no lo marque como bug inexplicado. | ANSI | ingeniería | §5.4 (L4 F4/F6) | decidida (igualdad de colección por contenido diferida) |
| DEV-053 | DIFERIDA: el SEND `printString` de String/Array NO entrecomilla en el MVP: `'foo' printString` ⇒ `foo` (sin comillas) y `#('a' 'b')` ⇒ `#(a b)` (elementos sin comillas), divergiendo de la forma literal ANSI (`'foo'` entre comillas; `#('a' 'b')`). La forma entrecomillada llega con el protocolo `printOn:`/`displayString` (diferido, L3-proper); `print.ts` es un bridge mínimo. Registrado para L6. | ANSI | ingeniería | §5.4 (L4 F5); §5.3 (printOn: diferido) | decidida (printString entrecomillado diferido) |
| DEV-054 | DIFERIDA (minor): un `Stream` respaldado por `OrderedCollection` coacciona la species de `contents` a `Array` (`(ReadStream on: anOC) contents class` ⇒ `Array`), porque `streamBufferOf` sólo ramifica en `isString`/`isArray` (e `isArray` matchea OC, que comparte el campo `elements`); un respaldo `Interval` se rechaza. GATE-F6 sólo exige respaldos `Array`/`String`, así que OC/Interval son fuera-de-contrato; los datos son correctos, sólo la species no se preserva. Registrado para no marcarlo como bug. | dialecto/MVP | ingeniería | §5.4 (L4 F6) | decidida (species de stream OC/Interval diferida) |
| DEV-055 | El corpus de conformance vive bajo `test/corpus/` (mismo árbol que los tests) en vez de un directorio raíz `conformance/` independiente. Decisión adoptada en L0/L1 antes de que el harness L6 la codificara; se mantiene por coherencia con los importes ya presentes en `corpus.test.ts` y `conformance-schema.test.ts`. | — (decisión interna de layout) | ingeniería | L6 (S1: feat(l6)) | implementada (`test/harness/conformance-schema.test.ts` CORPUS_DIR) |

| DEV-056 | `startsNumberAfterSign` ELIMINA la rama `.dígito`: `'-.5'` ya NO se tokeniza como float `-0.5`; en cambio produce `binarySelector('-')` + `period('.')` + `number(5)`, SIMÉTRICO con `'.5'` (que ya producía `period+5` antes). La asimetría antigua era una extensión silenciosa sin documentar: el lexer aceptaba `-.5` como literal negativo pero `+.5` no tenía contraparte. El float negativo con parte entera sigue funcionando (`'-0.5'` ⇒ float `-0.5` correcto). La cobertura de `-.5` como forma negativa-sin-entero queda FUERA del contrato del lexer MVP (ANSI tampoco la mandaba como literal independiente). | ANSI / dialecto | ingeniería | §2 (L1 S4); lexer R2/CORR-1 | implementada (`test/L1/s4-lexer-parser-majors.test.ts` #19) |
| DEV-057 | `evalSequence` inicializaba TODOS los temporales a `nil` incondicionalmente, sobreescribiendo bindings de parámetros que ya existían en el scope cuando un temporal tenía el mismo nombre que un parámetro (e.g. `[:x | | x | x] value: 42` retornaba `nil`). Se añade un guard `if (!ctx.scope.vars.has(temp.name))` para que sólo se inicialicen temporales cuyo nombre no exista ya (decisión de "el binding pre-existente gana"). ANSI no prohíbe redefiniir un parámetro como temporal local; pandi-sm elige preservar el binding del parámetro. | ANSI (silencioso) | ingeniería | §5.3 (L2/L3, eval.ts); sweep S5 | implementada (`test/L2/s5-eval-runtime-loader-majors.test.ts` #13) |
| DEV-058 | `identityHash` para `SmallInteger` usaba dos fórmulas distintas según el tipo JS: `number` ⇒ `v \| 0` (trunca a int32), `bigint` ⇒ `v % 0x7fffffffn` (módulo 31 bits). Esto violaba el contrato `identical(n, BigInt(n)) ⟹ identityHash(n) === identityHash(BigInt(n))`: para `n = 2^31` la primera da `-2147483648` y la segunda `1`. Se unifica a `Number(BigInt(v) % 0x7fffffffn) \| 0` para ambos tipos, garantizando la consistencia hash/identidad. | Smalltalk (contrato hash) | ingeniería | §5.4 (object.ts); sweep S5 | implementada (`test/L2/s5-eval-runtime-loader-majors.test.ts` #16) |
| DEV-059 | `activate` (el activador de `CompiledMethod` de usuario en `method.ts`) no verificaba aridad: si se pasaban más o menos argumentos de los declarados, los extras se descartaban silenciosamente y los faltantes se rellenaban con `nil`. Se añade un guard de aridad idéntico al de `evalBlock`, que invoca `signalError` cuando `args.length !== meta.params.length`. Los primitivas BUILT-IN (no `CompiledMethod`) siguen sin verificar aridad por diseño (son flexibles por su contrato de bajo nivel). | Smalltalk (error de aridad) | ingeniería | §5.4 (method.ts, activate); sweep S5 | implementada (`test/L2/s5-eval-runtime-loader-majors.test.ts` #20) |
| DEV-060 | El scanner de profundidad de corchetes `[`/`]` existía en DOS lugares: `splitMethodDefs` en `kernel-loader.ts` (cargador) y `splitPattern` en `method.ts` (compilador de método). Ambos contaban `$[`/`$]` (char-literals de Smalltalk) como corchetes de bloque, causando un balance incorrecto y un falso error de "cuerpo sin cerrar". Se añade un skip `if (c === '$') { i++; continue; }` en los dos scanners, de modo que el caracter que sigue a `$` nunca se trata como delimitador. | ANSI (char-literals $X) | ingeniería | §5.4 (kernel-loader.ts, method.ts); sweep S5 | implementada (`test/L2/s5-eval-runtime-loader-majors.test.ts` #21) |
| DEV-061 | `subclassFull` y `subclassShort` (los primitives que implementan `subclass:`) llamaban a `makeClassWithMetaclass` incondicionalmente, sobreescribiendo silenciosamente una clase existente si el nombre ya estaba en el namespace. Se añade un guard `if (u.namespace.has(name)) signalError(...)` antes de la creación, emitiendo un Error de Smalltalk capturable. Declarar la misma clase dos veces en la misma sesión es un error de programación; ANSI/Pharo señalan error. | ANSI/dialecto | ingeniería | §5.4 (primitives.ts, subclassFull/subclassShort); sweep S5 | implementada (`test/L2/s5-eval-runtime-loader-majors.test.ts` #22) |

## Próximos pasos

- Al implementar cada decisión, mover su fila a `implementada` y enlazar el caso
  de test que la fija.
- Añadir filas nuevas cuando una capa descubra una divergencia no anticipada
  (p.ej. retornos *unspecified* de `Stream` en L4).
